{
  "name": "@vcoder/desktop-shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm run build && electron dist/main.js",
    "start": "electron dist/main.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vcoder/shared": "file:../../packages/shared",
    "electron": "^40.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
